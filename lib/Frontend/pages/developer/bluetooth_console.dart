import 'package:flutter/material.dart';

class BluetoothConsole extends StatelessWidget {
const BluetoothConsole({Key? key}) : super(key: key);

@override
Widget build(BuildContext context) {
return Container();
}
}
